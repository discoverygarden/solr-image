{{- if eq .Values.storage.type "hostPath" }}
apiVersion: v1
kind: PersistentVolume
metadata:
  name: drupal-islandora-data
  labels:
    component: drupal
    usage: islandora-data
spec:
  storageClassName: manual
  capacity:
    storage: 20Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: {{ .Values.storage.basePath }}/{{ .Release.Name }}/drupal-islandora-data
    type: DirectoryOrCreate
{{- end}}