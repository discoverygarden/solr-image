apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: drupal-private-claim
spec:
  storageClassName: manual
  selector:
    matchLabels:
        component: drupal
        usage: private-filesystem
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
