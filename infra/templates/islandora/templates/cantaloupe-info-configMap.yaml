apiVersion: v1
kind: ConfigMap
metadata:
  name: cantaloupe-info-{{ .Release.Revision }}
  namespace: default
data:
    info: |
        fallback: http://localhost/islandora/object/%{pid}/datastream/%{dsid}/view?token=%{token}
        sitemap:
            drupal_d8_default: 'http://svc-drupal.{{ .Release.Namespace }}.svc.cluster.local%{suffix}'
