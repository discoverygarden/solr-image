apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "islandora.fullname" . }}-test-connection"
  labels:
    {{- include "islandora.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "islandora.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
