apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: solr-data-claim
spec:
  storageClassName: manual
  selector:
    matchLabels:
        component: solr
        usage: data
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
