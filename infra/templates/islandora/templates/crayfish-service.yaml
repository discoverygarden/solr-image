apiVersion: v1
kind: Service
metadata:
  name: svc-crayfish
  labels:
    component: crayfish
spec:
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: 80
  selector:
    component: crayfish
